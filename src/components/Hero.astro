<section class="hero">
  <div class="container hero__inner">
    <h1 class="hero__title">We buy small profitable<br />SaaS businesses</h1>
    <p class="hero__subtitle">
      We step in so founders can move on, certain that their customers
      are cared for and their legacy will thrive.
    </p>
    <div class="hero__ctas">
      <a href="/contact" class="btn-primary">Talk to us</a>
      <a href="/process" class="btn-outline">Learn more</a>
    </div>
  </div>
</section>

<style>
  .hero {
    background-color: var(--color-primary-green);
    padding: 100px 0 120px;
    text-align: center;
  }

  .hero__inner {
    max-width: 720px;
  }

  .hero__title {
    color: var(--color-white);
    font-size: 3.5rem;
    margin-bottom: 20px;
  }

  .hero__subtitle {
    color: var(--color-white);
    font-size: 1.15rem;
    opacity: 0.9;
    margin-bottom: 40px;
    line-height: 1.7;
  }

  .hero__ctas {
    display: flex;
    gap: 16px;
    justify-content: center;
    flex-wrap: wrap;
  }

  @media (max-width: 768px) {
    .hero {
      padding: 60px 0 80px;
    }

    .hero__title {
      font-size: 2.2rem;
    }

    .hero__subtitle {
      font-size: 1rem;
    }
  }
</style>
